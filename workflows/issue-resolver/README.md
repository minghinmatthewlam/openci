# Issue Resolver

Converts labeled issues into draft pull requests using an AI coding agent.

## Install

```bash
npx openci add minghinmatthewlam/openci --workflow issue-resolver --provider codex
```
